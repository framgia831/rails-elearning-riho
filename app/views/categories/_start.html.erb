<%= button_to "Start now!", category.lessons.build(user: current_user), 
                                    params: {category_id: category.id}, 
                                    class: 'btn btn-primary btn-block' %>